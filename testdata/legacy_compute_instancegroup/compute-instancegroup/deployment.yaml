resources:
- name: compute-instancegroup
  properties:
    description: Compute instance group with two specified instances and named http
      and https ports.
    namedPorts:
    - name: http
      port: 8080
    - name: https
      port: 8443
    zone: us-west1-a
  type: compute.v1.instanceGroup
- name: compute-beta-instancegroup
  properties:
    description: Compute beta instance group with two specified instances and named http
      and https ports.
    namedPorts:
    - name: http
      port: 8080
    - name: https
      port: 8443
    zone: us-west1-a
  type: compute.beta.instanceGroup
